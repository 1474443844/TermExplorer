Place your precompiled arm64-v8a 'coreutils' binary here and rename it to 'libcoreutils.so' so that it is packaged directly inside the APK!
Note: Android requires native libraries to have a 'lib' prefix and '.so' suffix to be packaged. Our app will automatically extract and symlink it.
